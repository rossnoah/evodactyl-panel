{
  "name": "@pterodactyl/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "migrate:resolve-baseline": "prisma migrate resolve --applied 00000000000000_baseline",
    "studio": "prisma studio",
    "db:pull": "prisma db pull",
    "seed": "bun --filter @pterodactyl/api run cli seed"
  },
  "prisma": {
    "seed": "bun --filter @pterodactyl/api run cli seed"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0"
  },
  "devDependencies": {
    "prisma": "^6.6.0",
    "typescript": "~5.7.0"
  }
}
